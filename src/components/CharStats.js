import React from "react";

const CharStats = () => {
  return (
    <div className = "stats">
      <select></select>
      <select></select>
      <select></select>
      <select></select>
      <select></select>
      <select></select>

    </div>
  )
}

export default CharStats;