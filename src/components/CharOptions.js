import React from "react";

const CharOptions = () => {
  return (
    <div className="options">
      <select></select>
      <button>rand</button>
      <select></select>
      <button>rand</button>
      <select></select>
      <button>rand</button>
      <select></select>
      <button>rand</button>
    </div>
  )
}

export default CharOptions;